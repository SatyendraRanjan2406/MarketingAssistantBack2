# Clean Requirements - Only What's Actually Used
# Generated by analyzing actual imports in the codebase

# Core Django Framework
Django==5.2.5
djangorestframework==3.14.0
django-cors-headers==4.3.1

# JWT Authentication (Required for Django REST framework)
djangorestframework-simplejwt>=5.3.0

# Database
psycopg2-binary==2.9.9

# Google Ads API
google-ads==28.0.0
google-auth==2.40.3
google-auth-oauthlib==1.2.2
google-api-core==2.25.1

# Google API Client (for OAuth services)
google-api-python-client>=2.0.0

# OpenAI Integration (for DALL-E and GPT)
openai>=1.0.0

# LangChain (for AI chat and tools)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.10

# Vector Database (for knowledge base)
chromadb>=0.4.0

# File Processing (for knowledge base)
python-docx>=0.8.11
PyPDF2>=3.0.1
beautifulsoup4>=4.12.0

# File Monitoring (for knowledge base)
watchdog>=3.0.0

# HTTP Requests
requests>=2.32.0

# Environment Variables
python-dotenv>=1.0.0

# Data Processing (minimal - only if actually used)
numpy>=1.24.0
pandas>=2.0.0

# Image Processing (Pillow - only if actually used)
Pillow>=10.1.0

# Background Tasks (Celery - only if actually used)
celery>=5.3.4
redis>=5.0.1

# Development Tools (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
